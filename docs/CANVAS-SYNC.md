# Canvas sync — approved changes the export has not made yet

Changes the founder approved and this repository implements, which the design
canvas has **not** made. Each one therefore diverges from the export on purpose,
so each carries a named exemption in `tools/render_gate.py` (with the same
justification) and appears here.

The point of this file is that the divergence stays visible. A change listed
here should end up in the canvas and arrive by re-export; when it does, delete
its row **and** its gate exemption in the same commit, so the gate goes back to
measuring that section.

| # | Change | Where implemented | Gate exemption |
|---|---|---|---|
| 5 | Sector pages' Related projects section rendered with the canvas **Ledger** component (`.lg`), four-column variant: ID / Project / Role for *[Sector]* / Status. Same rows and data as the list it replaces. The component's `<style>` block was moved into the shared stylesheet — CSP is `style-src 'self'`, so an inline `<style>` would not apply, and the extraction rule holds regardless. Class names unchanged, so it drops in when the canvas ships it in `sections.css`. | `website/src/css/integration.css`, `website/src/sectors/sector.njk` | `sectors/ :: Related projects` |
| 6 | Homepage S5: drop the closing note ("Water is next…") and move the "All fifteen sectors" button into its position, with the vertical gap the note used to provide restored beneath the sector grid. | `website/src/index.njk`, `_data/home.json` | `index.html :: S5 Where we start` |
| 7 | Sector hero illustration **sized to fit its column** on all 15 pages. The first attempt (a fixed slot height, ~2x) made the box taller than the column was wide, so `object-fit: contain` letterboxed the artwork and the empty space read as the illustration wrapping below the title. Width now drives: the artwork fills the column and the slot takes its height from the fixed 1531x1029 aspect — 564x379 at 1440 against the original 432x290 (+30% linear, +70% area), no letterboxing at any width, and the identical-Y guarantee verified at 279px across all 15. | `website/src/css/integration.css` | `sectors/ :: Sector head` |
| 8 | `/projects/` — "Every project has to earn its cost claim." matched to the page's other section-heading size. | `website/src/css/integration.css` | `projects.html :: How they help` |
| 10 | Footer: the "RSS" link is removed — no feed or feed page exists. | `_data/chrome.json` | *(chrome; no section exemption — the footer sits outside `<main>`)* |
| 11 | Footer: the bottom bar no longer repeats links already in the columns above it. Deduplicated by **target**, not label, since the columns carry "Corrections & changelog" pointing at the same `/receipts/#corrections`. Only "Accessibility" remains. | `_data/chrome.json` | *(chrome; outside `<main>`)* |
| 13 | Nav labels: "Sectors" → "Cost of Living Sectors" (The Case); "Dashboard" → "Human Essentials Dashboard" and "Tracker" → "Cost of Living Tracker" (The Projects); "How it works" → "How the Assembly works" (The Assembly). "Dashboard" removed from The Case, ambiguous against the Projects entry. | `_data/chrome.json` | *(chrome; outside `<main>`)* |
| 14 | `/accessibility/` — new page carrying the accessibility statement that was the bottom section of About; the footer "Accessibility" link now points at it. **Resolved 2026-08-22:** the slot About was left with now carries the UNP-82 v1 §2 member-voice section (“You get a genuine say, not a spectator seat.”); no marker remains. | `website/src/accessibility.njk`, `about.njk`, `_data/chrome.json` | *(new route; no export counterpart)* |
| 12 | `/receipts/` — the register band takes the whiter cream; each figure's title links to its registered source URL (all 22 rows have one). **Amended 2026-08-22:** the earlier removal of the "A figure, fully dressed" section is reversed — UNP-82 v1 §4c dresses it with a real register row (SRC-023) instead of the export's `$X` placeholder, so the section returns without the illustrative markers or the placeholder disclaimer. | `website/src/receipts.njk`, `tools/post_patch_pages.py` | `receipts :: The register`, `receipts :: Worked example` |
| 9 | `/receipts/` — add the ink headline below the "The rule, three ways" eyebrow: "If we can't show where a number came from, we don't print it." The export ships the eyebrow straight into the three-column grid with no headline. | `website/src/receipts.njk` | `receipts :: The rule` |

| 15 | ~~`/receipts/` — one-line publishing rule under the H1.~~ **Reverted 2026-08-26 at the founder's direction: the hero reads only "The Receipts".** The line itself stands as canon; it simply does not belong in the hero. Nothing to sync — the page now matches the export here. | *(none — reverted)* | *(exemption removed)* |
| 16 | `/receipts/` — a one-line note that the four confidence labels and the sector/project status badges are deliberately separate systems. Closes the conflict UNP-82 v1 escalated. | `website/src/receipts.njk` | `receipts :: Confidence labels` |
| 17 | `/receipts/` — the corrections launch-state no longer says “no public figures have been published yet”, which contradicted the 22-row register rendered above it on the same page. Badge follows: “Log begins at launch” → “No corrections logged yet”. | `website/src/receipts.njk` | `receipts :: Corrections` |
| 18 | `/receipts/` — the register lead said “Eleven sourced figures” against 22 rendered rows. The count is now rendered from the register, so it cannot drift again. | `website/src/receipts.njk` | `receipts :: The register` |
| 19 | `/news/` — **Cost Watch is no longer a hand-curated reading list.** It renders the register’s own `placement=news-feed` rows, so each entry is a sourced, dated figure under the same rule as the rest of the site. The export’s three hardcoded third-party links and its “inclusion isn’t endorsement” note went with that model. | `website/src/news/index.njk`, `website/src/_data/register.js` | `news :: Cost Watch`, `news :: News — title` |
| 20 | `/sectors/` — the intro band carries the approved copy: it names all fifteen sectors and states the year-one order (Shelter, Food, Energy, then Water) instead of the one-line summary. | `website/src/sectors/index.njk` | `sectors :: Sectors — title` |
| 21 | Project pages ×7 — every dossier field now carries differentiated, founder-approved copy from `_data/projectContent.js` instead of shared boilerplate, and the “Related sectors” section gains a primary/secondary role line. Stage chips read **Draft** rather than the export’s “In development”, which every dossier’s `status: draft` contradicts. | `website/src/projects/project.njk`, `website/src/_data/projectContent.js` | `projects/* :: Project head`, `At a glance`, `Overview`, `Funding & status`, `Related sectors`, `Project CTA` |
| 22 | `/about/` — the slot vacated by the accessibility statement carries the UNP-82 v1 §2 member-voice section on the Assembly and the privacy commitment. | `website/src/about.njk` | `about :: Your say` |
| 23 | `/downloads/the-case-for-uncost.pdf` — the 14-page publication is committed and served first-party, hash-pinned in `docs/CONTROL.md`. No on-page download block yet: its copy is in the v2 content document, which has not reached the repository. | `website/assets/downloads/`, `website/eleventy.config.js`, `docs/CONTROL.md` | *(asset; no section)* |
| 24 | `/movement/` — **full copy rewrite** (UNP-82 v2 (a)): identity and offer, not audience-capture strategy. The export's "How it reaches people" (distribution mechanics) and the three Pledge/quiz/share-a-stat cards are gone; the body is now what the movement *is*, what taking part means, and the Assembly. Block 1 uses the export's own `.blk--first` in place of the introband, because v2 writes it as eyebrow + lead + body rather than the introband's h2 + p. The ink block's button row is dropped — v2's specified `[Sign the Pledge] [Get updates]` are the coral CTA directly below, which already renders exactly those two. | `website/src/movement.njk` | `movement :: What this is`, `What we stand for`, `What taking part means`, `How to take part` |
| 25 | `/case/` — UNP-82 v2 (b) additions: **b1** the mechanism argument as a new section after the hero (SRC-020 + SRC-024 through the register macro); **b2** the six-step "How a cost gets uncosted" method inside the export's own Methodology drawer, alongside "The rules" and "Confidence labels"; **b3** the closing lead above the unchanged "Evidence first. Tools next."; **b5** the PDF download line under the hero lead. | `website/src/case/index.njk` | `case :: The mechanism`, `The Case — title`, `Methodology drawer`, `Case CTA` |
| 26 | `/case/` — **content restoration, not a design change.** The v4.3 re-derivation replaced the essay page with the export's dashboard preview and dropped the register-backed "What living costs" section with it. Under the standing authority split the export owns look and the repo owns content, so the section is restored from `origin/main` @ `94d2481` and rendered in the export's receipts vocabulary via `figure.njk`. Carries four real figures — SRC-004, **SRC-021** (v2's b4, directly after SRC-004), SRC-023, SRC-005 — placed after the b1 mechanism section, before the dashboard-preview material. The export's KPI/basket `$X` placeholders stay exactly as shipped: they are the honest labelled preview of a tool that isn't built. Real figures argue the case; labelled placeholders preview the tool. | `website/src/case/index.njk` | `case :: What living costs` |
| 27 | `/news/` Cost Watch now renders **four** register rows, not one. Founder-confirmed 2026-08-22 after browser-rendered verification of each source: SRC-017 refreshed ($4.049, week ending 2026-08-17) and SRC-025/026/027 added (CPI-U all items +3.4%, food at home +2.7%, energy +14.7%, July 2026). SRC-023 refreshed to the July print and SRC-024 added in the same write. No template change was needed — `placement=news-feed` drives it. | `sources/register.csv` | `news :: Cost Watch` |
| 28 | **BUG FIX** `/sectors/` — the grid could not reflow and the right-hand column was pushed off-page. `1fr` tracks resolve to `minmax(auto,1fr)` and `auto` floors at min-content, which for a `.scard` is its illustration's intrinsic ~259px. Broken bands were **1041–1304 (5 cols), 681–784 (3 cols), 461–523 (2 cols)** — every one BETWEEN the gate's 1440/1290/390 sample points, which is why it shipped. Founder saw it as "card 05 renders but isn't visible". | `website/src/css/integration.css` | `sectors :: 15 sectors` |
| 29 | **BUG FIX + CANVAS BUG** policy pages — the whole policy layout (`.pol-wrap` TOC grid, sticky `.pol-toc`, `.pol-body` typography, `.callout`, `.vhist`) is scoped to `body[data-page="privacy"]`. **The export itself ships nine policy pages under `data-page="policies-<slug>"` and defines ZERO rules for that scope** — so the export's own policy pages render unstyled too. This is not a repo/export mismatch; it is a gap in the canvas. Our eleven routes now get the export's own block re-scoped, generated mechanically, declarations byte-identical. **Action for Claude Design:** scope the block to the policy pages the export already ships, then this row and its three gate exemptions retire together. | `website/src/css/integration.css` | `policies/* :: Policy header`, `Policy body`, `TOC` |
| 30 | Policy body — the export styles `.pol-body ul` but never `ol` (POL-010 §2 is an `<ol>`), and a 48-character slash-joined token in POL-010 set a min-content floor that overflowed every viewport under 476px. `ol` gets the export's own `ul`/`li` values; `min-width:0` + `overflow-wrap:break-word` fix the floor. | `website/src/css/integration.css` | `policies/* :: Policy body` |
| 31 | Missing ink divider at every cream→wheat boundary. `.blk` draws a coral rule and `.blk--coral` upgrades it to ink, but the export's wheat block sets `border-top:0 !important`. New repo-owned `.blk--wheat` carries background (from `--wheat`, the same value the export hardcodes) **and** the ink rule, so the templates drop `.u-87` and no longer depend on an extraction-class number. **Superseded 2026-09-12 by item 40** — the ink rule it added is removed; an ink rule under an ink band is invisible. | `website/src/css/integration.css`, `movement.njk`, `receipts.njk`, `case/index.njk` | `movement :: What we stand for`, `receipts :: The rule`, `case :: What living costs` |
| 32 | `/case/` reuses the receipts register card for its four figures (new `fig.card()` macro). The export scopes `.card` to `body[data-page="receipts"]`, so it is re-scoped mechanically. Two additions: two columns (four figures orphan the fourth in a 3-track grid) and `min-width:0` against the item-28 trap. | `website/src/css/integration.css`, `partials/figure.njk` | `case :: What living costs` |
| 33 | `/receipts/` worked example reuses `.fact`, the design's full-dress statistic — ink card, 104px coral numeral — so it reads as distinct from the cream register grid below it. Export scopes `.fact` to the homepage; re-scoped mechanically, plus a 640px width cap so a standalone card is not a full-bleed band. | `website/src/css/integration.css`, `receipts.njk` | `receipts :: Worked example` |
| 34 | `/case/` Methodology drawer headings promoted h4→h3 after item 36 removed the intervening h3s; the export's `h4` declarations are carried to `h3` so it is visually identical. | `website/src/css/integration.css`, `case/index.njk` | `case :: Methodology drawer` |
| 35 | **BUG FIX** `/projects/` and `/policies/` indexes were hardcoded export lists. Projects linked **2 of 7**; policies linked **0 of 11** (POL-001 pointed at the legacy `/privacy/` route) and — worse — published the **wrong reference numbers**: the export's POL-002 "Code of Conduct" is this repo's POL-003, off by one from POL-002 down. Both now loop `catalog`, so numbers, slugs and links are correct by construction. Project rows show the founder-approved `publicSummary`; policy rows show each policy's own "Applies (upon adoption) to:" scope line. Chips read **Draft**, matching the dossiers. | `projects/index.njk`, `policies/index.njk`, `_data/catalog.js` | `projects :: Now`, `projects :: Next`, `policies :: Policy set` |
| 36 | `/case/` — the export's dashboard preview (control bar, KPI cards, chart mock, cost basket; six `$X` placeholders) is removed. It previewed a tool that is not built and is tangential to the argument; the dashboard's home is the PRJ-001 page. Section backgrounds now alternate ink→cream→cream2→wheat→ink instead of running monotone cream. | `website/src/case/index.njk` | `case :: Controls`, `KPIs`, `Dashboard preview`, `Basket example` *(removed)* |
| 37 | `/movement/` opens with a display headline — "Technology should make living cheaper, not billionaires richer." — above the lead. Canon-safe: it is the homepage hero subline from `_data/home.json`. | `website/src/movement.njk` | `movement :: What this is` |
| 38 | Nav, Movement panel: "Sign the Pledge" → "Learn about the movement" (→ `/movement/`); panel description drops "pledge" → "Join in — learn, take part, spread it". | `_data/chrome.json` | *(chrome; outside `<main>`)* |
| 39 | **BUG FIX** register presentation — SRC-015 and SRC-018 carried the caption's opening **year** in the big-figure slot (`"2024,"`, `"2025,"`), which passed the verbatim guard precisely because the caption starts with it. Now `$182.7 billion` and `745,652`; the year still shows in the period slot where it belongs. `_data/register.js` gained a build-time guard so a bare year can never occupy the figure slot again. | `sources/register.csv`, `website/src/_data/register.js` | `receipts :: The register` |
| 40 | **DIVIDER RULE (supersedes item 31).** Every boundary between content bands carries exactly ONE rule: CORAL, unless a coral band is on either side, in which case INK. The export already implements most of it (`.blk` defaults coral, `.blk--coral` switches both edges to ink, `.blk--coral + .blk` follows). Five gaps closed in `integration.css`: the intro/status/legend bands drew the boundary from their BOTTOM edge while the band below drew it again from its top (6 doubles); `titleband`/`.pol-hd` → content drew nothing (16 routes); `.u-15`/`.u-154`/`.u-87` zero the rule with `!important`; `.u-93` tints a coral band's rule wheat; and item 31's own ink rule on wheat was invisible under an ink band — the "/ has no divider" report. Audited at 246 real boundaries (background-change pairs only, component grids excluded): **before** 136 coral / 56 ink / 35 none / 6 double / 13 wrong-colour → **after** 188 coral / 58 ink / 0 / 0 / 0. | `website/src/css/integration.css` | *(site-wide; no single section)* |
| 41 | `.hl` — AA-safe accent span for light bands. The export's `.u-1` uses `--coral`, measured at 4.04:1 on cream and 3.70:1 on cream2, both under the 4.5:1 AA floor for text. `.hl` uses `--coral-deep` (5.59:1 / 5.12:1), the same substitution `post_patch_css.py` already makes for links. Used by B1 and C1/C3. | `website/src/css/integration.css` | `case :: The mechanism`, `movement :: What this is`, `What taking part means` |
| 42 | `/case/` figure captions 14.5px → 29px, bold. Scoped to `/case/`; the receipts register keeps 14.5px where 26 cards must stay scannable. | `website/src/css/integration.css` | `case :: What living costs` |
| 43 | `/case/` Methodology drawer toggle is the FAQ chevron (same 45°/−135° rotation) instead of a `+` glyph. Done in CSS so it survives re-derivation. The export's glyph is `--coral` on wheat = 2.34:1, under the 3:1 non-text floor; the chevron uses `--coral-deep` at 3.25:1. | `website/src/css/integration.css` | `case :: Methodology drawer` |
| 44 | `/movement/` one content measure. The first two sections constrain text to 64ch; the two below ran the full padded width (728px vs 1280px at 1440), so the ink "Three ways in" band read as breaking out of the page container. All sections now share 64ch — verified 727px at 1440/1290/1024 and 342px at 390. | `website/src/css/integration.css` | `movement :: How to take part`, `What taking part means` |
| 45 | **DURABILITY GAP — not yet closed.** The UNP-82 body copy on `/movement/` (the four-block rewrite) and `/case/` (the b1 mechanism section, b2 six-step method, b3 closing lead, b5 download line) lives directly in templates that `gen_static_pages.py` rewrites from the export. A re-derivation would delete all of it. The 2026-09-12 sweep's own edits ARE restated in `post_patch_pages.py`, so they fail loudly rather than silently if the copy beneath them is gone — but the copy itself needs its own patches or a `_data` content layer before the next export. | `tools/post_patch_pages.py` | *(process; no section)* |
| 46 | `/contribute/` "Tell us how you can help" retargeted `/join/` → `/contact/`, breaking a two-page loop (`/join/` pointed straight back at `/contribute/`). `/join/`'s own link is unchanged, so the pair now reads contribute → contact, join → contribute. `/contact/` verified usable with `formsOpen=false`: zero form elements, one mailto route, and copy covering corrections, press and partnership. Restated in `post_patch_pages.py` — contribute.njk is rewritten from the export. | `website/src/contribute.njk`, `tools/post_patch_pages.py` | `contribute :: Ways to contribute` |
| 47 | Sector hero alignment — the grid is `align-items:start` and the export pushes the illustration slot down with a margin-top, leaving the artwork 106px below the text block's centre at 1440 (82px at 1290, 49px at 1024). Now centred against the H1-through-metadata block; the artwork already fits that height at every width, so no width reduction was needed. Measured centre-delta 0 at all three widths; Y spread across the 15 is 0.50px (Healthcare and Care sit 0.5px lower, from a 1px-taller metadata row). | `website/src/css/integration.css` | `sectors/* :: Sector head` |
| 48 | Sector "02 · Evidence status" moved from ink to cream2 — it sat on the same ink as "01 · Scope" above it, so the two read as one slab. `--white` is not a token, so cream2 is the choice. The four confidence chips carry their own backgrounds and text colours, so their contrast pairs are untouched; only the dashed empty-state card was ink-specific (border `--cream`, text `#C9C5BA`, both invisible on a light band). A1 rules on both sides follow automatically — audit re-run at 261 boundaries, still 0 none / 0 double / 0 wrong-colour. | `website/src/css/integration.css` | `sectors/* :: Evidence status` |
| 49 | **AUDIT FINDING — not fixed (audit-only instruction).** All 15 sector pages render the SAME section-03 heading: *"The worked example: fresh produce in a confined urban space."* — Food's scenario. `_data/sectorContent.js` carries a per-sector `scenario` field, but `sectors/sector.njk` hardcodes Food's. Of the 15: Food is correct; 10 show Food's heading above their OWN dossier-derived Match mechanism; and Healthcare, Care, Education and Safety show Food's heading with no Match step at all (their steps are measurement-only, deliberately, because their dossier guardrails forbid intervention work). Fix is a one-line template change to use `sectorContent[slug].workedExample.scenario`, plus a founder decision on the four gapped sectors. | `website/src/sectors/sector.njk` | `sectors/* :: Worked example` |
| 50 | `/assembly/` participation-loop boxes — the export paints the four box interiors `--cream` and the band they sit on is also `--cream`, so they were literally the same colour as their background. Interiors now cream2 (same token choice as item 48); ink borders, 2px grid gaps and coral numerals unchanged. AA re-checked on cream2: numerals 3.70:1 at 36px/900 (large text, 3:1 floor), h3 16.54:1, body 12.33:1 — all pass, and the numerals were 4.04:1 large-text-only on cream before, so no regression. | `website/src/css/integration.css` | `assembly :: What supporters can do` |
| 51 | **R1 extends the divider rule (supersedes the colour half of item 40).** The rule at each boundary is the first of [coral, ink, wheat] that differs from BOTH adjacent bands: cream↔wheat = coral, anything↔coral = ink, **ink↔coral = wheat** — neither coral nor ink is visible against both. Fixed `/movement/` "Three ways in" → "The simplest first step", which was an ink rule on an ink band. Audit: 257 boundaries, 199 coral / 57 ink / 1 wheat, 0 none / 0 double / 0 wrong. | `website/src/css/integration.css` | *(site-wide)* |
| 52 | **R1 named exception.** `/news/` intro → the article and Cost Watch columns is a same-background boundary that still carried a full-width coral rule until e08bcc9, when item 40 removed the introband's bottom edge and `.newswrap` had no top edge to take it over. Restored explicitly. | `website/src/css/integration.css` | `news :: News — title` |
| 53 | **Enumerator fix (not just the page).** The band audit missed boundaries in two ways, both now closed: a band's background can be painted by a full-width CHILD rather than itself (`.statusband`, `.u-106`, `.newswrap` all compute `transparent`), and an INSET element is a component, not a band — `/policies/` `.high` has horizontal margins and its own 2px ink frame, so its edges are never band boundaries. Bands are now required to span the full width of `<main>`. This also reverted item 40's `.u-46 + .u-8` rule, which had recoloured `.high`'s frame top edge coral. | *(audit tooling)* | *(process)* |
| 54 | **R2 COMPONENT-FRAME RULE (new).** Any grid or panel drawing ink divider lines between its cells carries a complete frame — top and bottom at the same weight and colour as those dividers. Six components drew dividers but left the frame open, so the outermost cells bled into the band: `/join/` `.tiers` (no frame at all), `/about/` `.parts4` (bottom only), `/contact/` `.cwrap` (no frame), `/contribute/` `.ways` (open bottom), `/pledge/thanks/` `.next` (open bottom, coral top), `/sectors/` `.sgrid` (4px coral top). All now 2px ink on four edges. Where a frame edge is flush against a band boundary the frame edge IS the boundary, so R1 is suppressed there — the audit recognises framed components and reports `ok_frame`. | `website/src/css/integration.css` | *(site-wide components)* |
| 55 | **R3 ONE FIGURE CARD.** Every sourced statistic renders as the receipts register card, evolved: SRC id; coral numeral; the data period directly under it (the period IS the figure's scope, so it is never repeated as a chip); the caption bold at 36px echoing its own display value in the numeral's colour; a SOURCE eyebrow with publisher and linked title; and a chip row of CHECKED · PUBLISHED (only where the register has a year) · region · confidence. Cells alternate cream/cream2 — odd/even for 3-track grids, the 4n pattern for 2-track, or a 2-column grid stripes instead of checkers. `.fact` is retired (item 33). The caption echo is built in `register.js` as `captionHtml`, escaped before the span is injected, so it is safe to render unescaped and can never carry markup from the register. Applied to `/receipts/`, `/case/` 2×2, `/case/` mechanism 2-up (closing B2 as option C), the sector figure slot (template only — no sector carries figures yet), and the **homepage**, which was not in the brief's list but used `.fact` and so had to move. | `partials/figure.njk`, `_data/register.js`, `css/integration.css`, `index.njk`, `receipts.njk`, `case/index.njk`, `sectors/sector.njk` | `receipts :: The register`, `case :: What living costs`, `case :: The mechanism`, `index :: S2 The problem` |
| 56 | `/receipts/` (P2) — the "A worked example / A figure, fully dressed" section is removed entirely; the register band takes a wheat background; "current" in "Built to stay current." is accented with `--coral-on-ink` (the band is ink, so `--coral-deep` would be unreadable there). Supersedes items 12 and 33. | `website/src/receipts.njk`, `css/integration.css` | `receipts :: The register`, `receipts :: Keeping current` |
| 57 | **R4 PROSE-BAND RULE (new).** A text-only band — eyebrow + headline + body, no grid or table — uses one style everywhere: headline at `clamp(34px, 4.2vw, 56px)`/700 wrapping at its natural width (the size `/projects/` "Gated on funding and safety." already used), body on the 728px/64ch measure. Colour and background stay per page. Before: prose headlines ranged 40–88px and bodies 376–840px. After: **100** headlines at the reference size (was 7) and **61** bodies at 728px (was 6). Multi-column bands are excluded with `:has()` — "Three ways in" and every card/table grid untouched, as directed; the 38 headlines still at 72px are all in grid-bearing bands. | `website/src/css/integration.css` | *(site-wide prose bands)* |
| 58 | **R5 TABLE RULE (new).** The sector "04 · Related projects" Ledger (`.lg`) is the reference tabular format: 2px ink frame, ink header row, 2px ink row dividers, cream2 alternating rows. Enumerated six tabular lists site-wide — `.lg` (reference), `.conftable` (/receipts/), `.vhist` (policy pages), `.plist` (/projects/) and `.pollist` (/policies/) already conform; sector "05 · Sources" was a hairline list with no frame and no header and is **the one converted**, with its own ID · Source columns. Its confidence value is left in the meta copy rather than split into a status chip, because separating it would mean rewriting copy. Restated in `post_patch_pages.py` — sector.njk is re-derived. | `website/src/sectors/sector.njk`, `css/integration.css`, `tools/post_patch_pages.py` | `sectors/* :: Sources` |
| 59 | P3 `/case/` Methodology drawer: body +25% (14→17.5px), the three column headings 2× and bold (12→24px/700). | `css/integration.css` | `case :: Methodology drawer` |
| 60 | P5 sector "02 · Evidence status": the separator above the REGISTER line becomes ink — the export drew it `--cream` for an ink band and item 48 moved the band to cream2, leaving it invisible. **Same move left `.u-143` at `#C9C5BA` on cream2 — 1.48:1, unreadable** — so the REGISTER label and text move to the light-band foreground at 12.33:1. | `css/integration.css` | `sectors/* :: Evidence status` **Corrected 2026-09-25 (C15): this was recorded as resolved and was not.** The REGISTER label was moved off `--rule-soft`, but `/treasury/`'s KPI placeholders (`.kpi .pending`, the em-dash at up to 64px) were left on it and composite to `#C8C4BA` on cream2 — **1.49:1** against a 3.0 bar, the same unreadable value this row names. Found by `tools/contrast_audit.py` measuring the rendered page. Now `--fg-3`, 6.18:1. |
| 61 | P4 sector heroes. **The export's H1 already overflowed**: `clamp(72px,12vw,180px)` renders 173px at 1440, but "Transportation." needs 970px of a 724px column there, so the long names ran into the illustration. A literal +25% is impossible for them, so the size is per-sector — +25% where it fits, largest-that-fits where it does not. Seven names are capped, eight take the full +25%; H1 overflow is now 0 at 1440/1290/1024. Subtitle +50%. The illustration uses a BASELINE spec replacing item 47's centring: each artwork's drawn ground line sits level with the hero rule above "SECTOR NN / 15". The ground line was measured per file — it sits 16.33%–18.56% above the image's bottom edge, a 2.24-point spread — so each page carries its own `--gp`. Worst delta **0.5px** at all three widths, against a 4px tolerance. | `css/integration.css` | `sectors/* :: Sector head` |
| 62 | P6-copy: PRJ-001 and PRJ-002 carry a founder-approved "At a glance" opener (eyebrow + headline + intro) from `_data/projectContent.js`. The other five projects keep the export's plain "At a glance" eyebrow. | `_data/projectContent.js`, `projects/project.njk` | `projects/* :: At a glance` |
| 63 | P1 `/policies/` — "in the open" accented with `--coral-deep` in "Version-controlled, in the open." | `policies/index.njk` | `policies :: How policies change` |
| 64 | P6 `/receipts/` keyword filter — a client-side box that filters the register cards in place. The bar ships `hidden` and only `/js/keyword-filter.js` reveals it (renamed and generalised under N5, item 73), so with JS off the full register renders and no dead control is shown. Not inside a `<form>`; nothing submits and nothing is fetched (verified: 0 network requests while filtering). External file, not inline, because CSP is `script-src 'self'`. The search overlay gains a "Search the Receipts" button; the "Site search is planned" copy is untouched. | `receipts.njk`, `src/js/keyword-filter.js`, `partials/search.njk`, `css/integration.css` | `receipts :: The register` |
| 65 | **P6 SECTOR CHIPS — BLOCKED, not built.** The filter chips were to be per-sector, but `sources/register.csv` has no sector column and nothing else maps SRC ids to sectors: only Food carries a `headlineSrc` in `_data/sectorContent.js`, and no sector dossier references an SRC id. Building the chips would mean authoring that mapping, which is a register change and founder-gated. **Unblock:** add a `sector` column to register.csv (or an SRC→sector map in `_data/`), then the chips are a small addition to the existing filter. The keyword box already covers the behaviour in the meantime. | *(not built)* | `receipts :: The register` |
| 66 | **BUG-1** `/receipts/` filter matched but never hid. `.cards .card{display:flex}` is (0,2,0) and beats the reset's `[hidden]{display:none}` at (0,1,0) — the same cascade trap as the `.cards` responsive regression in R3. Scoped to the card rather than a blanket `[hidden]` rule, so it cannot mask the next instance. The test now asserts on RENDERED cards (`offsetParent !== null`) instead of the counter text, which was the thing reporting success while hiding failed. | `css/integration.css` | `receipts :: The register` |
| 67 | **BUG-2** `/join/` double line. R2 says a framed component's edge flush with a boundary IS the boundary, but only the AUDIT was taught that (item 54) — the stylesheet still drew the component's ink frame edge AND the next band's rule. Fixed generically: one selector list is now the single source of truth for framed band components, feeding both the frame and the boundary suppression, so the two cannot drift. `.sgrid`/`.tiers` were also removed from the audit's name-based exclusion — they are full-width framed BANDS, and the full-width test now handles them. Frame audit tightened to gap ≤ 4px, since a 12px+ gap is spacing, not a drawn divider. Result: 16 divider components, 0 incomplete; band audit 257 boundaries, 0 problems. | `css/integration.css` | *(site-wide components)* |
| 68 | **C1 ONE CORAL, BY BACKGROUND.** Driven by the BAND, not by class name — `.u-1`, `.num`, `.n` and `b` each appear at both display and small sizes, so a class-by-class rule cannot be made correct. Per-band default, safe at BOTH the 3:1 large bar and the 4.5:1 small bar: cream/cream2/white → `--coral-deep` (5.59/5.12/5.99); ink → `--coral-on-ink` (6.42); wheat → `--ink`; **coral → `--ink`** (coral on coral is 1.38:1, invisible — 78 occurrences of the sector six-step numerals were in this state). Display accents then take the lighter `--coral`, re-asserted only through HEADINGS so they are always large: 4.04 cream / 3.70 cream2 / 4.47 ink against a 3:1 bar. **Wheat is the named exception** — `--coral` is 2.34 there, below even the large bar, so wheat display accents keep `--coral-deep` (3.25). Audit: 64 distinct coral text pairs, all green. | `css/integration.css` | *(site-wide)* |
| 69 | **C1 CONFLICT — resolved and flagged.** The brief says all small text stays `--coral-deep` on every background, but on WHEAT that measures **3.25:1**, below the 4.5:1 small-text bar — and no coral value clears it (`--coral` 2.34, `--coral-on-ink` worse). Small text on wheat therefore resolves to `--ink` (10.48:1). This is the only place C1 could not be applied as written. | `css/integration.css` | *(wheat bands)* |
| 70 | **C2 SECTION LAYOUT STANDARD.** Text-only bands are stacked (eyebrow → headline → body → optional button). The homepage "Point the tools at the things that keep people alive." band was the **only** outlier site-wide — `.intent` is a 1.15fr/1fr grid with the headline left and body/button right. Now stacked at the R4 scale with the 728px measure. The ten `/policies/` `.pol-wrap` layouts also read as two-column but are a contents sidebar beside a document body, not a prose band, so C2 does not apply. | `css/integration.css` | `index :: S3 What we intend to do` |
| 71 | **C3 FIGURE CARD REFINEMENTS** (shipped as variant 1). Caption 36→31px. The data period moves out of the slot under the numeral into the chip row as SCOPE, so it appears exactly once: CHECKED · PUBLISHED · SCOPE · REGION · CONFIDENCE. Chip text to `--ink` (the export's `--fg-2` read light at 10.5px). Metadata chips take coloured borders over a neutral fill; confidence chips keep solid fills and remain the only solid-colour chip, so colour still means confidence. **Border palette:** confidence owns sage, wheat, ink-blue and coral, so the only values in the token set provably distinct from all four are the neutral ramp (`--ink`/`--fg-2`/`--fg-3`) — that ships; C4 explores hues. Date chips colour by RECENCY computed at build time (current/prior/older), never by literal year, so no new colour is needed each January. Region chips colour by region code — all 26 rows are United States today, so one colour shows. All chips AA-green on both cell colours (16.54/18.06 metadata, 7.11 confidence). | `partials/figure.njk`, `_data/register.js`, `css/integration.css` | `receipts :: The register`, `case :: What living costs`, `index :: S2 The problem` |
| 72 | **N4 COST WATCH ITEM CARD + LABEL GUARD.** A Cost Watch entry is not an R3 figure card: the figure card claims "this number is ours, checked on this date, at this confidence", the Cost Watch card claims only "this publisher printed this, then". It keeps the export's compact `.cw` block and adds one footer row — the outbound "Read at &lt;domain&gt;" on the left, one label on the right. **Reported** is that label, the only one used anywhere under `/news/`: same shape as a confidence chip, deliberately no colour, because colour means confidence everywhere else on this site. A build-time guard fails on either leak — a `.rcpt-conf` label under `/news/`, a `.lbl--reported` label outside it, or `confidence="reported"` in `sources/register.csv`. It reads class markers, not prose, so the English words "reported" and "confirmed" stay free in body copy. Proven by introducing each leak deliberately. | `partials/news-cards.njk`, `eleventy.config.js`, `css/integration.css` | `news :: Cost Watch` |
| 73 | **N1/N2/N5 NEWS SPLIT LANDING AND ITS TWO ROUTES.** `/news/` becomes a split landing: the latest 3 Uncost updates on the left, the latest 5 Cost Watch items on the right, each column with its own head, its own founder-approved one-line intro, its own keyword filter and its own link to the full list. The single combined intro sentence is gone — an intro that describes two things describes neither. The updates column now renders from `_data/news.js` instead of the export's three hardcoded placeholders, so the page and the feed cannot disagree. `/news/uncost/` and `/news/cost-watch/` are new routes on the same header pattern and the same `pageId`, so the export's page-scoped news rules apply unchanged; `.newswrap--solo` drops the sidebar chrome when Cost Watch is not a sidebar. Cost Watch gets its own RSS at `/news/cost-watch/feed.xml` alongside the existing feed. Heading levels differ by route (h3 head + h4 item on the landing; introband h2 + h3 item on a dedicated route), so the item rules are restated for both. `keyword-filter.js` replaces `receipts-filter.js` and serves every list. | `news/index.njk`, `news/uncost.njk`, `news/cost-watch.njk`, `news/cost-watch-feed.njk`, `_data/register.js`, `_data/chrome.json`, `js/keyword-filter.js`, `css/integration.css` | `news :: Updates`, `news :: Cost Watch` |
| 74 | **C1 FOLLOW-UP — two backgrounds the C1 pass never judged.** The C1 audit classified each text run by the NAMED token behind it and printed anything else unclassified rather than judging it. Two export bands paint a hardcoded hex near a token but not equal to one — `.u-7` `#F5AB2E` (wheat family, `/about/` "Is / is not") and `.u-45` `#F2EBE1` (cream family, `/faq/` answer body) — so they fell out of the count. Measured, both failed: `--coral-deep` on `#F5AB2E` is 3.06:1 for a 13px eyebrow, and `--coral` on `#F2EBE1` is 3.19:1 for 16px answer links. Resolved by the rules C1 already set: the wheat-family band follows item 69 (small text to `--ink`, display accents keep `--coral-deep`), the cream-family band follows the C1 default (small text to `--coral-deep`). The audit's classifier now resolves an unnamed background to its own hex and judges it, so a hardcoded hex cannot be counted as neither pass nor fail again. | `css/integration.css` | `about :: Is / is not`, `faq :: Questions` **Corrected 2026-09-25 (C15): the rule was right and was not fully applied.** It reached `.u-7 :is(h1,h2,h3) .u-1` and `.u-7 .hl` — accent SPANS inside a heading — but `/about/`'s "How a cost gets uncosted." is `h2.u-4.sec`, a heading carrying the accent colour itself, so no selector matched it and it kept shipping `#D64A1F` on `#F5AB2E` at **2.21:1**. Now `--coral-deep`, 3.06:1 — which clears the large bar by 0.06 and holds only because the heading is 52-56px and can never be small text. |
| 75 | **C1 ON A HOVER STATE — the Cost Watch card.** `.cw:hover` repaints the card wheat, changing the background under every text run in it; the C1 pass measured resting states only. Measured on hover, `--coral-deep` on wheat is 3.25:1 for the 11px publisher/period line and `--fg-3` on wheat is 3.91:1 for the 12px "Read at …" line. Both are small text, so item 69 applies exactly as it does to a wheat band and both resolve to `--ink` (9.60:1). The card title and the Reported chip were already compliant. Hover states are still not covered by the standing coral audit; the three news routes are now checked in both states. | `css/integration.css` | `news :: Cost Watch` |
| 76 | **G2 FOLLOW-UP — six `ch` measures survive the one-measure rule; three are body text.** G2 (`6c89607`) made R4's prose measure absolute (`64ch` → `728px`) and gave the `.blk > p` measure `!important`, but only those two rules. Six `max-width` declarations in `css/integration.css` are still `ch`-relative, so each renders a different px width at every font-size — the defect G2 exists to remove. **Body text (same defect class, unfixed):** `:196` `.pol-body` 76ch, the policy document body on all eleven routes; `:939` `body[data-page="index"] .intent p` 64ch, which G2's selector list does not match, so it survives; `:1484` `body[data-page="news"] .newsxref` 78ch, the container of 15px body copy. **Not body text (no action needed):** `:190` `.pol-hd h1` 20ch — a headline on the display font at `clamp(34px,5vw,60px)`, where capping by character count is the intent, not the defect; `:1046` `.up-item :is(h3,h4)` 26ch — a subhead at a **fixed** 26px, so the ch measure cannot vary. **Dead as of `6c89607`:** `:447` `body[data-page="movement"] .blk > p` 64ch is now overridden by G2's `!important`, leaving only the `.blk > .path` half live. `.path` is a layout div of `h3`+`p` step blocks — **not monospace; this stylesheet defines no monospace family at all** — and it sits in R4's `:not(:has(…))` exclusion list precisely because it is layout, not prose, so its cap is a layout cap and not a measure. | *(not built — follow-up)* | *(none)* |
| 77 | **C1 — INK IS `#0A0A0A`, NOT THE EXPORT'S `#0E0E0C`.** The two are visually indistinguishable; the whole difference is what clears AA. On `#0A0A0A`, `--coral` as text measures **4.58:1** and ink on a coral fill measures **4.58:1**, so both pass the 4.5 floor at EVERY size. On `#0E0E0C` both were **4.47:1** and failed it below display size — which is why the export needed a separate `--ink-on-coral` token at all. That token now holds the same value as `--ink` and is kept only so the export's own rules and the adoption patches keep resolving; it is no longer a correction. `--rule`, `--rule-soft` and `--rule-hair` follow, since they are defined as ink at reduced weight. **The token alone is not enough:** the export also writes the ink hex as a raw literal no token change reaches — nine times in `site.css` (the `.ft-updates` footer block) and once in `sections.css` (`.u-105`'s ink background). Left behind they would render a second, slightly different ink beside the token one, which is the unnamed-hex problem item 74 had to work around. All ten are corrected and re-applied by `tools/post_patch_css.py` after every adoption. **Supersedes the coral contrast rule in `design-system/AUTHORITY.md` §Text on a coral fill**, which still describes `--ink` as 4.47 and unsafe on coral. | `website/design-system/tokens.css`, `site.css`, `sections.css`, `tools/post_patch_css.py` | *(site-wide; render gate exemptions named per ink band)* |
| 78 | **E1 — the caption echo takes `--coral`, the numeral's shade.** Item 85b set this in 2026-09-19 and it never took effect: `.echo` also sat in C1's per-band default list (item 68, rule 1), which is `!important`, so every echo kept rendering `--coral-deep` beside a `--coral` numeral on the same card. Measured on the rendered page, every echo is **31px/700 at 1440 and 20px/700 at 390** — WCAG large text at both ends — so it leaves that list and the 85b rule carries `!important`. On its cells: 4.04 cream / 3.70 cream2 against a 3.0 bar. Corrects the prior session's claim that echoes were 14.5px — that was inferred from `.card .claim` in the CSS, not measured. | `css/integration.css` | `receipts :: The register`, `case :: What living costs`, `case :: The mechanism`, `index :: S2 The problem` *(already exempt)* |
| 79 | **E2 — the homepage hero is one coral.** "Uncost the cost of living." (`h1 .u-50`) rendered `--coral-deep` `#B23A14` while "not billionaires richer" (`.hl`) on the line above rendered `--coral` `#D64A1E`. C2 listed the hero for `--coral` and missed this line: `.u-50` sat in C1's per-band `!important` list, so it now leaves that list and joins the display-accent rule. Verified on the rendered page at 1440 (77.8px) and 390 (38px): both lines `#D64A1E`. h1 text, so the bar is 3.0; `--coral` on cream is 4.04. The word "Uncost" is `.u-51` and declares its own colour, so D5 governs it separately. | `css/integration.css` | `index :: S1 Hero` |
| 80 | **D1 — link hover by ground (founder decision 2026-09-25, option a).** Hover and the current-page state (`a.here`, which the export styles in the same rule as `:hover`) repaint links `--coral`: 4.04:1 on cream, under the 4.5 bar for 13–16px links — 476 hover runs and 7 `a.here` runs on the 2026-09-25 baseline. Now **light grounds (cream, cream2, white) → `--coral-deep`** (5.59 / 5.12 / 5.99); **ink grounds (footer, ink bands, project header, sector anchor bar, title bands) → `--coral`** (4.58), because `--coral-deep` is 3.31 on ink and fails small text. Rest colours unchanged. Implemented as one rule rather than twenty: the hovered link redefines `--coral` for itself to a ground token (`--hover-coral`), so every export hover rule written as `color: var(--coral)` resolves to the right shade. The ink scopes were found by walking every link's ancestors on 25 routes at 1440 and 390. Buttons excluded — their hover repaints a fill. Contrast audit **531 → 48**, no new pair. | `css/integration.css` | *(hover/chrome state; no section changes at rest)* |
| 81 | **D2 — `span.redact` stays as it is (founder decision 2026-09-25).** The `/treasury/` example redaction bar is ten U+2588 FULL BLOCK glyphs in ink on ink — **1.00:1 by design**, because it depicts a redaction. Text content measured by `tools/contrast_audit.py --show redact`: `██████████`, **no real words**, so D2's condition for `aria-label="redacted"` is not met and none is added. It is a **named exemption** in `contrast_audit.py`, keyed on route + selector + the exact colour pair, so any colour change fails the audit again. The audit gained the mechanism with this item: exercised exemptions are listed with their measured ratio, and an exemption whose route is audited but which matches nothing is a `STALE EXEMPTION` error. Adversarial selftest cases: an exempted run passes; the same selector with a different ink fails; an unmatched exemption fails. | `tools/contrast_audit.py` | *(none — no rendering change)* |
| 82 | **D5 — hero "Uncost" keeps `#E8B84A` (founder decision 2026-09-25).** `span.u-51`, the word "Uncost" at the start of the homepage hero's second line, stays `--wheat` `#E8B84A` on cream: a **founder-accepted AA exception at 1.72:1** against the 3.0 large-text bar (77.8px/700 at 1440, 38px/700 at 390 — measured by `contrast_audit.py --show`). Named exemption in `tools/contrast_audit.py`, keyed on the exact colour pair. The prior session's D2 "77.8px decorative numeral" was this same element, so D2 and D5 together cover two elements, not three. The decision text carried two alternative swaps (`#A77B15` at 3.58:1, `#B78717` at 3.02:1); neither was selected, so neither is applied. | `tools/contrast_audit.py` | *(none — no rendering change)* |
| 83 | **D4 — accent headlines on wheat keep `--coral-deep` (founder decision 2026-09-25, option a).** The **one named exception to the one-coral rule**: on wheat `--coral` is 2.34:1, below even the 3.0 large bar, so display accents there take `--coral-deep` at **3.25:1**. Already implemented by item 68 rule 3 (headings only, so always large text); this row records it as a decision rather than a workaround. Measured with `contrast_audit.py --show`: homepage "keep people alive" and `/movement/` "price tag", `#B23A14` on `#E8B84A` at 56px/700 (1440) and 34px/700 (390). Options (b) ink + coral underline and (c) band to cream2 were not chosen. | `css/integration.css` (comment only) | *(none — no rendering change)* |
| 84 | **D3 — the three full-bleed band bodies stay unframed (founder decision 2026-09-25).** `/about/` Mechanism step list (`main > div`), `/roadmap/` Year one phase list (`main > section`), `/pledge/` body beside the form (`.pwrap > div`). Each draws internal ink dividers but is a band body, not a component panel, so R2's complete-frame rule does not apply. **Named exemptions in `tools/frame_audit.py`**, reason: founder decision. Keyed on exact route + exact component path (each is the only bare element at that path on its page), covering only frame-edge findings — an `unresolved` finding still fails, and an exemption that matches nothing on a rendered route is a `STALE EXEMPTION` error. C14 listed `.pwrap > div` for a frame but deferred band bodies to D3; the D3 renders are this element, so it is exempted, not framed. | `tools/frame_audit.py` | *(none — no rendering change)* |
| 85 | **C5 — homepage hero spacing and a new line.** The headline's second statement ("Uncost the cost of living.") sits `.35em` lower than a wrapped line would — **27px at 1440, 13px at 390**, measured — so the two statements read as two. In the intro, "We aim to significantly…" **starts a new line after "…price tag."**: the space after the full stop becomes a `<br>` in `_data/home.json`. No copy changes. Because `tools/rederive_home.py` re-extracts `home.json` from the export on every adoption, the break is restated in `tools/post_patch_pages.py`, which fails loudly if the sentence has moved (verified: the patch regex reproduces the edit byte-for-byte from the pre-change file). | `css/integration.css`, `_data/home.json`, `tools/post_patch_pages.py` | `index :: S1 Hero` |
| 86 | **C6 — every lifecycle/status chip takes BRIEF's border.** BRIEF (`.status--planned`) draws `2px solid rgba(10,10,10,.18)` — ink at 18%, a soft rim of the chip's own fill. Enumerated every `.status` chip on every route at 1440 (7 families): **already BRIEF's** — `status--planned` (BRIEF, PLANNED, PLANNED TOOL, DRAFT, DRAFT — NOT APPROVED FOR BUILD, NOT APPROVED FOR BUILD, NOT YET ACTIVE / DRAFTED / OPERATIONAL, NO CORRECTIONS LOGGED YET, DONATIONS NOT OPEN, PUBLIC REVIEW DRAFTS) and `status--future` (FUTURE STUDY); **changed** — `status--dev` (IN BUILD, DRAFT, DRAFT — NOT IN FORCE), `status--focus` (FOCUS), `status--next` (NEXT), `status--dossier` (DOSSIER), and the `/sectors/` legend's `s-next`/`s-dossier` (2px ink) and `s-future` (2px `--fg-3`). Re-enumerated after: every chip renders `2px rgba(10,10,10,0.18)`. The value is pinned (`--status-rim`) rather than read from `--rule-soft`, because `.blk--ink` redefines that token to `#3A3A36`. Fills, text and dots unchanged, so the fill still carries the state. The Cost Watch "Reported" label and the figure-card confidence chips are not lifecycle chips and are untouched. | `css/integration.css` | `sectors :: Legend`, `sectors/* :: Sector head`, `projects/* :: At a glance`, `index :: S4 What we run` *(already exempt)* |
| 87 | **C7 — figure-card chip row: G5 variant 05, every metadata-chip border ink at one weight. FLAGGED FOR FOUNDER REVIEW.** Variant 05 was chosen from `review/2026-09-19-chips/05-two-tone--*`, and **those PNGs never showed variant 05**: its injected CSS was written at (0,5,0)/(0,6,0) and lost to item 85d's `span.m` / `span.m .k` at (0,5,1)/(0,6,1). All four contain **zero** pixels of its violet `#4A4A8C` (variant 01's contain 2,512 of its plum, because its border rule carried an attribute selector and won). What the founder chose from shows grey `#5A574F` labels, plain checkerboard fills, and borders in ink or grey. Shipped: exactly that, with C7's amendment — every metadata-chip border **ink at 1.5px** (the confidence chip's weight, so the row is one weight). Variant 01's hue and recency-weight rules are removed; recency is no longer drawn, `data-era` stays in the markup. Labels pinned to `#5A574F` (6.74 cream / 6.18 cream2), because an ink band redefines `--fg-3` to `#8E8B82`, 2.92:1 on the homepage's cards. The same specificity loss means **item 86's label hues never rendered either** — only its borders did. The variant's intended two-tone (violet dates on a tint, olive context) was never seen, so it is rendered correctly in `review/2026-09-25-c7/` for a decision, not shipped. | `css/integration.css` | `receipts :: The register`, `case :: What living costs`, `case :: The mechanism`, `index :: S2 The problem` *(already exempt)* |
| 88 | **C8 — nav dropdown illustrations centred on both axes, ~70% of panel height.** The export floors each illustration and caps it at 140px. Measured by the new `tools/nav_panel_audit.py` on all four panels (The Movement, The Case, The Projects, The Assembly), 2 routes × 1440/1290/1100: **before** — panel 268px, artwork drawn 105×140, **52.2%** of the panel, **54px below centre**; **after** — panel still 268px, artwork drawn 140.7×187.6, **70.0%**, **0px off-centre on both axes**, identical on every panel. The audit measures the DRAWN artwork (object-fit and object-position aware), not the `<img>` box, and judges centring against the wheat area inside the column's 2px border. `contain: size` on the column stops the image's percentage height feeding back into the row — without it the panel grew 268→304px. | `css/integration.css`, `tools/nav_panel_audit.py` | *(chrome; outside `<main>`)* |
| 89 | **C9 — `/join/` privacy strip → `--cream-2`.** "We won't sell your data or use your pledge for partisan targeting." moves from the export's `--wheat` to `--cream-2`, matching the three tier cards directly above it (measured: both `#F2EDE1`). Its text is ink, so no colour pair changes (`/join/` contrast: 0 failures). The tiers' 2px ink frame bottom is flush with the strip's top, so under R2 that frame edge is the boundary; strip → Interest form keeps the form's 4px coral top. | `css/integration.css` | `join :: div.privacyline` |
| 90 | **C11 — `/faq/`: the pale strip below the last question becomes wheat.** The strip was not a band: the coral CTA's `.u-47{margin-top:48px !important}` exposed `<main>`'s own `#F2EBE1` between the last wheat question section and the CTA (measured 1726→1774 at 1440). The 48px moves from the CTA's margin into the last section's bottom padding, so the gap is wheat and **the CTA does not move** (top at 1774 before and after at 1440; 1977 at 390). The wheat → coral boundary is then the CTA's own 4px ink top, as R1 requires, and the band audit's two findings on that gap's upper and lower edges go with it. | `css/integration.css` | `faq :: Governance` |
| 91 | **C12 — `/receipts/` "The rule" band → `#FFF`, its three cards → `--cream-2`.** New repo-owned `.blk--white` (the export has no white band class; `--white` is not a token). The template changes the band's **class** from `.blk--wheat`, restated in `tools/post_patch_pages.py`, because C1's wheat rules key on that class and would otherwise keep its eyebrow and 01/02/03 numerals ink on white. Measured: band `#FFFFFF`, cards `#F2EDE1`, eyebrow and numerals `--coral-deep` (numerals 5.12 on cream2), headline 19.8, card heads 16.95. The band above ("The register") is wheat, so a boundary now exists where there was none: wheat → white and white → cream2 both carry the 4px coral R1 requires. | `receipts.njk`, `css/integration.css`, `tools/post_patch_pages.py` | `receipts :: The rule` *(already exempt)* |
| 92 | **C10 — `/news/` and `/news/cost-watch/`: full-width list, cream-2 filter band, white cards.** Measured at 1440 before: column capped at 840px (item 76), cards 712px on `--cream-2`, filter bar unpainted. After: the cap goes and the list runs the container's full width (**cards 1312px** at 1440, 342px at 390); every item card is **`#FFF`**; the keyword filter bar is a **`--cream-2` band**; small coral text on the cards (`.date`, `.src`, "Read at … →") stays **`--coral-deep`, 5.99 on white** (`--coral` would be 4.32). Card text keeps the site's one wrap point, `min(66vw, 960px)` (C4) — a full-width card holding a ~190-character line of 16px copy would not be readable. The wheat hover (item 75) still outranks the white. Both routes: 0 contrast failures, rest and hover. | `css/integration.css` | `news :: div.newswrap` *(already exempt)*; `/news/cost-watch/` has no export counterpart |
| 93 | **C14 — complete R2 frames.** Built from `tools/frame_audit.py`'s findings: **`.steps6`** (sector six-step list, 15 routes: 2px ink dividers, frame open on four sides) → 2px ink frame; **`/assembly/ .rules`** (on ink, 2px **cream** dividers) → 2px cream frame, since R2 asks for the dividers' own colour; **`/treasury/ .kpis`** (dividers were each card's `border-right`, plus a 4px coral top per card, and at 390 the stacked cards were split by 4px coral) → one 2px ink gap grid with a 2px ink frame drawn by the band itself, so divider and frame are one line at 4, 2 and 1 columns; the per-card coral tops go, item 76c is superseded, and the panel below loses its own 2px top through item 67's list; **`/404 .e-wrap`** → 2px ink frame, pulled up 2px under the sticky header so its top edge and the header's line are one line, and the quick-links grid below loses its own top through item 67's list. Cells now flush against a frame get 24px horizontal padding. Frame audit on the affected routes: only `.pol-wrap` remains. **`.pwrap > div`**: exempt under D3 (item 84), not framed. **`.pol-wrap`: CONFLICT, NOT BUILT.** It sits flush under the ink policy header on all 11 policy routes. R2 makes its frame's top edge the boundary at the dividers' 2px ink — invisible against the ink header — where R1 requires one visible rule (coral, at ink → cream). Both cannot hold at that edge. Options for a decision: (a) accept an ink frame edge merging into the ink header, and teach the band audit that a frame edge against a same-colour band is closed rather than missing; (b) inset the 390 contents divider so `.pol-wrap` has no edge-to-edge divider and is not an R2 subject; (c) something else. **Also found, not in scope:** `/404`'s quick-links grid is a `<nav>`, which the frame audit skips as a landmark, so its open sides and coral bottom have never been audited. | `css/integration.css` | `sectors/* :: Worked example`, `assembly :: Standing rules`, `treasury :: section.`, `error-404 :: div.e-wrap` |
| 94 | **C13 — every band-audit problem fixed.** `tools/band_audit.py` on the unchanged baseline: **23 problems** (once its own stale-ink and 304 defects were fixed — as committed it reported 197). Cleared by earlier items: `/faq/` 48px gap ×6 (C11); `/404` e-wrap \| e-quick ×6 and `/treasury/` kpis \| panel ×6 (C14's frames — R2 makes each frame edge the boundary). Fixed here: **(a) `/about/` Mechanism → Principles double rule** (1440/1290/390) — the last step carried `.u-2`'s `!important` 2px ink bottom border on top of the ink band's 4px coral R1 rule; the step list is an unframed band body (D3), so its trailing line goes and R1's rule is the boundary. **(b) `/about/` "01–04" grid at 390** — every cell kept its divider borders on the grid's outer edges once it collapsed, stacking 4px of ink on the 2px item-54 frame (bottom audited; right edge stacked the same way, unaudited because vertical); dividers now run only between cells at 4, 2 and 1 columns. **(c) `/contact/` coral rule under the framed panel** — `.cwrap` is the last thing in `<main>` and the footer draws a 4px coral top on every page, so the frame's 2px ink edge sat on a coral line; item 67's R2 suppression now extends to the footer when a framed band component ends the page (only `/contact/` today). **No committed audit sees (c):** `band_audit` and `pixel_validator` both stop at `<main>`'s last child; checked by screenshot only. After: **834 boundaries, 0 problems.** | `css/integration.css` | *(about/contact changes below gate tolerance; see gate run)* |
| 95 | **F1 — the 18 section-headline misses C3 did not reach.** `tools/type_audit.py` found them after C3: `.sys > .card > .bd > h2` on the seven "on the way" pages (`/case/dashboard/`, `/case/tracker/`, `/community/`, `/news/events/`, `/press/`, `/quiz/`, `/share/`) at 40px/28px, and on `/pledge/thanks/` "Now make it count double." (22px) and "What's next?" (30px/22px) — against the modal 56px (1440) / 34px (390). C3's own comment called these card/component headings and left them alone; that was wrong — each `.sys` band holds exactly **one** card, so its heading is the band's headline, and the two `/pledge/thanks/` headings each head a band. They join C3's rule. `.sys` is excluded where it is the page's first band (the three `/pledge/` status pages), because there it is the hero. **Headline misses 18 → 0.** Measure mismatches 77 → 85 at this commit: the nine newly-scaled headlines carry C4's wrap point before their bodies do — F2 closes that. | `css/integration.css` | `later/* :: div.sys`, `pledge-thanks :: Share` |
| 96 | **F2 — the body measures C4 did not reach.** `tools/type_audit.py`: 77 prose bands with headline and body on different wrap points (85 after F1). C4's body rule is gated by R4's `:not(:has(.scope, .evd-empty, .fund, …))` grid list, so bodies in those bands kept the export's `ch` caps — sector `#scope` `p.u-139` 547.6px and `#evidence .evd-empty > p` 625.8px (15 routes each), project `.fund > .fb > p` 704/586.7px (7), "on the way" `.sys .card .bd > p` 524.9px (7), `/pledge/thanks/` `.share > p.sub` 547.6px — and on `/assembly/` two containers were narrower than the wrap point (`.bound` capped at 860px held its body at 800px; the "A look ahead" headline shared a flex row with a status chip and wrapped at 748px). Bodies now take `min(66vw, 960px)`; the three boxed bodies (`.fund`, `.evd-empty`, `.bound`) widen by exactly their own padding + border so their text reaches the same point; the preview headline's column fills its row; the centred share block stays centred. **Measure mismatches 85 → 0; headline misses 0.** | `css/integration.css` | *(see gate run)* |
| 97 | **Contrast residue — the last text under AA after D1–D5.** Found by `tools/contrast_audit.py` on the rendered site (20 failures once D1/D2/D5 were in): **(a)** figure cards are light surfaces wherever they sit, but on the homepage they sit in an ink band whose `--fg`/`--fg-2`/`--fg-3` redefinitions leaked in, turning the SRC id and SOURCE eyebrow `#8E8B82` — 3.18 cream / 2.92 cream2 (12 runs); the card restores `:root`'s light-ground values of those three tokens. **(b)** small `--fg-3` text on the wheat family, which item 69 resolves to ink wherever it reached: `/projects/` group headings on the export's hardcoded `#E7B84A` (3.90) and `/receipts/` `p.u-131` on the wheat register band (3.91), both 14px; ink is 10.70 / 10.73. | `css/integration.css` | `projects :: Now`, `projects :: Next`, `receipts :: The register`, `index :: S2 The problem` *(already exempt)* |
## Nits to fix in the canvas

Adopted as delivered rather than silently "corrected", so the canvas and this
repository do not drift:

- `ledger-table` uses a raw `#FFF` on `.lg` where `--white` exists, and a
  `#B23A14` literal as the `var(--coral-deep, …)` fallback on `.src a`. Both are
  the only raw hex values the component carries.

- `sectors.html` marks its three year-one cards `class="scard focus"`, but no
  export stylesheet has ever defined `.focus` — not v3.1, not v4.3 — so the
  modifier renders nothing and the three focus cards are visually identical to
  the other twelve. Recorded rather than invented: giving `.focus` a treatment
  here would be authoring design. It is the site's only undefined class
  reference (3 occurrences, all on `/sectors/`).

## Also worth folding into the canvas

Not founder-requested changes, but corrections this build carries that the
export keeps re-shipping. They are re-applied automatically by
`tools/post_patch_css.py` and `tools/post_patch_pages.py` after every adoption.

- **WCAG 2.2 AA** — the export puts `#FFFFFF` (4.32:1) or `--ink` (4.47:1) on
  brand coral and `--link: var(--coral)` (4.04:1) on cream. All below the 4.5
  floor. See `website/design-system/AUTHORITY.md`.
- **Focus-ring visibility** — a `--coral` ring on a coral fill measures ~1:1.
- **Heading levels** — the export skips a level on about, roadmap, the policies
  index and the project At-a-glance grid.
- **21 undefined classes** — v4.3's pages reference classes its stylesheets do
  not define (116 occurrences); recovered in
  `website/src/css/integration-v43-gap.css`. Worth an §A0 check: *every class a
  page references is defined by the stylesheets*.
